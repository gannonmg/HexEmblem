# macOS
.DS_Store

# Xcode — per-user state, never shared
xcuserdata/
*.xcuserstate

# Xcode build output
DerivedData/
build/
.derivedData/

# Swift Package Manager
.build/

# Local Working Docs (like Claude Agent hand off for specific features)
.localDocs/

# Do not store BattleAnimations on Github. Keep folders in place as example.
BattleAnimationCore/Sources/BAPlayback/Resources/SourceAnimations/
BattleAnimationCore/Sources/BAPlayback/Resources/ProcessedAnimations
*.bapack
